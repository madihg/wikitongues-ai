## Frontier fill LANDED: Gemini 3.1 Pro tops the leak-free table without retrieval (2026-08-13)

TASK: register the new frontier arms (Gemini 3.1 Pro, Claude Opus 5 - each as baseline / v1 RAG / rag-v2), generate their frozen-43 outputs through the exact serving paths the arena uses, rescore via leak-audit.

CODE: new `scripts/frontier-fill.ts` - a GENERIC runner (`register` / `generate [slug ...]` / `all`): for any candidate slug list it reproduces, per candidate, the serving path the arena would use - baseline = bare generateForCandidate; v1 = the chat route's assembly (searchRag k=4 + retrieveGoldExamples k=8 over the route's own gold-pool query, REAL prompt identity so the contamination guard fires as in prod, plus the hard own-gold spend refusal from igala-rag-run); v2 = sniff-test-v2's leg (buildRetrievalV2 on the human slug, buildUserTurnV2, IGALA_SYSTEM_V2, exemplars as prior turns). Idempotent (existing outputs skipped), resumable, per-candidate isolation with a 1-token probe so one dead provider cannot stop the others; retrieval cached per prompt across candidates. Shape spec lives in `src/lib/arena/frontier-targets.ts` (+ tests): all six arms temperature 0 / maxTokens 4096 (Gemini 3.1 Pro is a REASONING model billing its hidden trace against the completion budget - the Gemma 16/43-empty trap), rag-v2 rows carry parentCandidateId lineage to their v1 sibling. `pricing.ts` gained a claude-opus-5 row ($5/$25, must sit above the legacy claude-opus $15/$75 substring match). GATES: tsc 0, lint 0, 537/537 tests (2 new files).

REGISTERED: gemini-3-1-pro(-rag,-rag-v2) on google/gemini-3.1-pro-preview; claude-opus-5(-rag,-rag-v2) on anthropic/claude-opus-5. ARCHIVED both gemini-2.5-pro candidates (model closed to new projects; selectable dead rows in chat burned us before). Gemini 2.5 Flash rows untouched (real n=22 data).

RUN (2026-08-13): all three Gemini arms 43/43, zero failures, ~$0.54 rough spend (134,950 in / 135,856 out; gemini-3.1-pro-preview is not in pricing.ts so it bills at the $1/$3 default - treat as floor). No decodingParams adaptation needed - temperature 0 + maxOutputTokens 4096 accepted as-is. Claude Opus 5 arms SKIPPED at the probe: Anthropic returns HTTP 404 "Not Found" with an EMPTY response body (not a 401), consistent with a dead/deactivated key or org - rows stay registered, runner resumes the moment the key works (`npx tsx --env-file=.env.local scripts/frontier-fill.ts generate claude-opus-5 claude-opus-5-rag claude-opus-5-rag-v2`).

SCORES (leak-audit, strLF = stripped chrF on the 27 leak-free prompts): **Gemini 3.1 Pro PLAIN 37.2 [27.1-48.3] is the new table leader** - above its own RAG v2 33.4 [23.7-44.3] and RAG v1 32.2 [25.8-39.8], and far above GPT-4.1 + Igala RAG 22.9 [18.4-27.7] and GPT-4.1 plain 9.2 [6.8-12.5]. All three Gemini CIs overlap each other, so "retrieval hurts Gemini 3.1" is NOT established - but retrieval demonstrably does not help it the way it helps GPT-4.1 (9.2 -> 22.9). A frontier model now matches gold better bare than every retrieval-fed arm we have; the interesting next question is whether Agnes's live judgment agrees, and what the paired verdict says at this n.

## Automatic eval harness + measured autorater LANDED (2026-08-09)

THE PROBLEM IT SOLVES: 781 blind human comparisons have produced 5 decided winners, none involving the tuned model. There was no systematic way to say whether `cmsjptop00001jpzubdwvttxk` ("GPT-4.1 mini SFT (Igala cold-gold)") beats the frontier baselines. There now is an automatic signal, and its reliability is a measured number rather than an assumption.

CODE: new `src/lib/eval/` - `chrf.ts` (chrF/chrF++, Popović 2015/2017, sacrebleu-compatible: char orders 1-6, word orders 1-2 for ++, beta=2, whitespace stripped, clipped counts, per-order F averaged over effective orders, max over references), `similarity.ts` (diacritic-exact / tone-insensitive / fully-folded match + token edit similarity), `normalize.ts` (Igala's two diacritic classes: SEGMENTAL ẹ/ọ vs TONAL - folded separately, never together), `reference.ts` (multi-gold best+mean scoring and the leave-one-out INTER-GOLD CEILING), `langid.ts` (char n-gram Naive Bayes over Igala/Yoruba/Igbo/English/Pidgin + its own k-fold cross-validation), `stats.ts` (percentile bootstrap, PAIRED bootstrap for head-to-head, Wilson intervals), `runner.ts` (pure composite report), `autorater.ts` (verdict rule + validation against the 781 human labels), `collect.ts` (the one Prisma-touching file), `README.md` (what each metric can and cannot tell us). Surfaces: `scripts/run-eval.ts` (CLI table / `--json` / `--write`), `GET /api/arena/eval` (researcher-gated, 5-min in-process cache), `/admin/arena/eval` + `src/components/arena/auto-eval.tsx`, new "Automatic eval" tab in `arena-nav.ts`. 132 new unit tests; typecheck + lint + 386 tests green.

RESULTS on the frozen 43 (2026-08-09 18:36, corpus still growing as other agents generate): INTER-GOLD CEILING chrF **63.2 [58.0-68.1]** - that is the limit, not 100. GPT-4.1 + Igala RAG **26.8 [22.5-31.2]**; tuned SFT **25.4 [18.0-34.4]**; Llama 3.3 70B + RAG 20.7; Gemini 2.5 Flash 12.7 (n=22); GPT-4.1 10.4; GPT-4.1 mini 9.0; Llama 3.3 70B Instruct 8.1; Gemma 4 31B IT 5.2 (n=25). Paired deltas: **tuned vs GPT-4.1 +15.0 [8.2, 22.4]**, vs GPT-4.1 mini +16.4 [8.4, 25.0] - both real. **RAG vs tuned +1.4 [-6.8, 9.0] = NOT DISTINGUISHABLE.** So: tuning clearly moved the model toward community gold; it has NOT been shown to beat retrieval over that same gold.

LANGUAGE GATE: tuned model 88.4% [75.5-94.9] of outputs called Igala vs 16.3% (GPT-4.1), 9.3% (GPT-4.1 mini), 37.2% (GPT-4.1+RAG). Gate reliability MEASURED by 5-fold CV on held-out text: 81.4% Igala-vs-English (n=1318), 81.9% on the binary "is it Igala?". Yoruba/Igbo/Pidgin profiles are seed lexicons with NO validation data - triage flags only, and the report says so everywhere.

AUTORATER HONESTY (the headline): agrees with the human label on 90.5% of 781 comparisons - which is WORSE than the 99.2% a constant "both inadequate" predictor scores, and Cohen's kappa is **-0.014**. It has learned the base rate and nothing else. Only 5 comparisons name a winner, only **2** of those sit on a gold-backed prompt, and the autorater matched 0. ~97 decided comparisons are needed for a ±10pt interval. Conclusion: the autorater is usable as a regression alarm, NOT as a ranker.

TWO REAL BUGS FOUND AND FIXED WHILE BUILDING: (1) per-profile add-alpha smoothing let a 120-word seed lexicon out-score a 937-answer profile - only 488/937 Igala gold classified as Igala even trained on itself; switching to a SHARED vocabulary took that to 847/937 and CV accuracy 49.5% -> 81.4%. (2) an empty model output was classified as Igala by the softmax tie-break; `noEvidence` now makes an empty answer neither Igala nor English (Gemma's apparent Igala share fell 84.6% -> 24.0%).

CONSENT GAP FOUND AND CLOSED: `ColdAuthorAnswer.consentBenchmark` ("you may use my answer to benchmark models") is a SEPARATE permission from `consentTraining`, which `sft-source.ts` and `gold-retrieval.ts` already honour. Nothing in the codebase read `consentBenchmark` - this harness is the first benchmark consumer of gold, i.e. the consumer that flag was written for. `collect.ts` now enforces `consentBenchmark: true` in the QUERY (so no code path can reach a non-consented answer) for BOTH uses of gold: scoring references AND the Igala language profile, since the profile is a component of the benchmark. 8 answers excluded, all currently on NON-holdout prompts, so 238 references and the 63.2 ceiling are unchanged; the Igala profile corpus goes 937 -> 929. The count is reported in the CLI and the page's stat strip (`corpus.goldExcludedNoBenchmarkConsent`), never silently dropped. The guard matters because annotators are working right now and a frozen prompt can gain a non-consented answer at any time. ENFORCED BY TEST, not by comment: `src/lib/eval/collect.test.ts` injects a recorder in place of Prisma and asserts the actual WHERE clauses - gold read with `consentBenchmark: true`, exactly ONE gold query so no second unfiltered read can reintroduce it, demo excluded on every table, holdout-only prompts. Verified to FAIL by deleting the filter and watching it fire, then restored; a guard nobody has seen fail is not a guard.

CAVEAT THAT MUST TRAVEL WITH THE NUMBERS: chrF rewards writing like these annotators write. The frozen prompts were excluded from training (leakage verified 0 previously), so this is not contamination - but a model fine-tuned on this community's gold has been taught this community's orthographic habits, and chrF pays for exactly that. Read the tuned model's lead as "writes more like these speakers", which is necessary and nowhere near sufficient. Only blind native judgment settles quality.

## Together FULL fine-tune LANDED and is UN-SERVABLE: the real rule found (2026-08-09)

TASK: full (non-LoRA) fine-tune of Llama-3.1-8B on Together, deploy on a v2 dedicated endpoint, generate the frozen 43, tear down. STOPPED at the servability gate AFTER training, BEFORE any GPU spend. **Total spend $4.00 (training only). Zero GPU-minutes. Zero endpoints created - `tg beta endpoints ls` is empty, both v1 endpoints STOPPED and belong to another project.**

WHAT RAN: DB job `cmsm4lwzn0001jpcbl0fvmxdc` -> Together `ft-177f8a45-1335`, base `meta-llama/Meta-Llama-3.1-8B-Instruct-Reference`, **`training_type: {"type":"Full"}` confirmed on the job object** (a real full fine-tune, not the LoRA default), SFT, 692 rows, 3 epochs, batch 8, lr 1e-5, 169,281 tokens, **$4.00 billed** (`total_price` 4000000000 nano-USD). Trained in ~9 minutes. Output `madihalim_2eb2/Meta-Llama-3.1-8B-Instruct-Reference-14570f6c` = v2 model `ml_CdseZkgPUsdGz6kYpv85P`, 32.1 GB of weights. Dataset grew 573 -> 692 rows (937 cold answers now); **237 held-out source rows dropped; frozen leakage 0 by prompt id AND by prompt text; 0 lines missing the system turn.** Candidate `cmsm51qdn0001jp5iykvcepz8` auto-registered then **archived** and renamed "[UNSERVABLE - no deploy config]" (0 outputs), its EvalRun marked failed, so the arena is unchanged.

THE BLOCKER, and the general rule nobody had: the trained checkpoint's `baseModelId` is `ml_CcoJYxxyrZAYqyTqDBD6Y` (the training-only 8B object) which has **zero** deployment configs. Together refuses, verbatim: `No configs found for model ml_CdseZkgPUsdGz6kYpv85P.` and, when forced with the production config, `Config cr_Cd35GMEk4PNy5Ly6yMGEK is not valid for model ml_CdseZkgPUsdGz6kYpv85P.` Both refusals allocate no GPUs, so this cost $0 to establish.

**The rule: a fine-tune is servable iff ONE v2 registry entry carries BOTH `PRODUCT_FINE_TUNING` and `PRODUCT_DEDICATED` with a certified config** - then the training object and the deployable object are the same object, so the output inherits the config. `meta-llama/Llama-3.1-8B-Instruct` is `PRODUCT_DEDICATED` ONLY; its fine-tuning twin is a separate object. This is why the pre-spend probe passed and still misled: registering a model against the PRODUCTION base `ml_CcoJJ5xjwdLjhbg5aCcCB` DOES inherit certified config `cr_Cd35GMEk4PNy5Ly6yMGEK` (1x H100) - but a fine-tune never lands there. Checking "does the architecture have a certified config" is the WRONG test. `tasks/open-model-options.md` flag 2 called this exact mapping unproven; it is now disproven.

DECISION PENDING (did not substitute a model, per the task's explicit gate): the corrected base is **`Qwen/Qwen3.5-9B`** - both products on one entry, base `ml_CbuqU8KKAtoGrowco4nqK`, certified config `cr_Cd35Fpam3FrMdwHdmroZD` (1x H100 80GB), `supports_full_training: true`, same shape as the 70B fine-tune that provably deploys on this account. Another ~$4 to train, ~$4 of GPU for a 45-minute serve window. One command: `TOGETHER_BASE_MODEL=Qwen/Qwen3.5-9B npx tsx --env-file=.env.local scripts/together-full-sft-run.ts create|preview|estimate|launch|poll|servable|serve`. Salvaging the existing 8B checkpoint instead would mean downloading 32 GB and re-uploading it under the servable base - documented and legal, but hours of transfer.

CODE: `together.ts` gained full fine-tuning (`lora:false` -> `training_type:{type:"Full"}`; the API takes a tagged OBJECT, not a boolean), `fetchModelLimits`, `estimateJobPrice` (Together quoted $4.00/220,667 tokens in advance - note it returns PLAIN USD while the job returns NANO-USD), `fetchSupportedModels` + `selectDeployableFineTuneBases` (the rule above, unit-tested against the real registry rows), and `TOGETHER_BASE_MODELS` cut to the two bases that can actually be served, with `TOGETHER_UNSERVABLE_BASE_MODELS` as a named graveyard. **Routing bug fixed:** `register-fine-tune.ts` pointed Together checkpoints at `api.together.xyz/v1`, which answers any tuned model id with 400 model_not_available; dedicated endpoints live at `https://api-inference.together.ai/v1` and are addressed by the ENDPOINT STRING. New `scripts/together-full-sft-run.ts` (create|preview|estimate|launch|poll|servable|serve|teardown|metrics|arena) puts deploy+prove+generate+teardown in one try/finally and resolves the deploy config from the CHECKPOINT, never from the base. `assertServableAfterTraining` now blocks launch before the file is even uploaded. `scripts/together-endpoint-guard.sh` extended to v2 (it only watched v1, which is blind to exactly the endpoints that have no idle shutdown).

GATES: typecheck 0, lint 0, test 395/395 (30 files; new `together.test.ts` 14).

## Together serverless-LoRA verification: STOPPED before training, no such product exists (2026-08-09)

TASK: train an open-weights Igala LoRA on Together using a base whose adapters Together serves SERVERLESSLY (no dedicated endpoint), because open weights matter independently for community release + interpretability, and the Qwen3-14B run ($4.00, `ft-e63efcfc-8670`) proved un-servable. STOPPED at step 1 (verification) per the task's explicit gate - did not spend the $10 budget, did not touch `fine-tune-providers.ts`/`together.ts`, did not create any endpoint.

VERDICT: Together currently has **no serverless LoRA inference product at all** - every fine-tuned checkpoint (LoRA or full, any base model) requires either a dedicated endpoint (billable, GPU-reserved, this account cannot create one) or a local download. The Dec-2024 "Serverless Multi-LoRA" feature (the togethercompute launch tweet/blog people still cite) is gone from the current docs; multi-adapter serving now lives under Dedicated Endpoints ("Serve multiple LoRA adapters on one endpoint" - explicitly: "Only dedicated endpoints, not serverless, can be used as adapter targets").

EVIDENCE (4 independent sources, all fetched live 2026-08-09):

1. `docs.together.ai/docs/fine-tuning/deployment.md` - "Once a fine-tuning job completes, your model is available for inference in two ways: hosted on a dedicated endpoint at Together AI, or downloaded as a standalone checkpoint." No serverless path mentioned.
2. `docs.together.ai/docs/dedicated-endpoints/lora-adapter.md` - "Only dedicated endpoints, not serverless, can be used as adapter targets. Only private endpoints can be used as adapter targets."
3. `docs.together.ai/docs/fine-tuning/supported-models.md` - full LoRA-fine-tunable list (29 bases incl. `Meta Llama 3.1 8B Instruct Reference`, `Llama 3.3 70B Instruct Reference`, `Qwen3.5` family, `Mixtral 8x7B`, `GPT-OSS`, `GLM 5.1`, `Kimi K2.x`, `Gemma 4`) carries no serverless-after-tuning marker anywhere.
4. **Empirical, on THIS account**: called `POST /v1/chat/completions` with `model: "madihalim_2eb2/Qwen3-14B-92ef7bd3"` (the real registered adapter id from the earlier Qwen3-14B run) - live response today: `400 model_not_available` - `"Unable to access non-serverless model madihalim_2eb2/Qwen3-14B-92ef7bd3. Please visit https://api.together.ai/models/... to create and start a new dedicated endpoint for the model."` Same wall as before, confirmed still standing.
5. Cross-checked programmatically: `tg beta models public --product fine-tuning` (23 models) vs `--product serverless` (2 models: `deepseek-ai/DeepSeek-V4-Flash-0731`, `thinkingmachines/Inkling-Small`) - **zero overlap**. Nothing fine-tunable on this account's catalog is also serverless-tagged.

Also fixed in passing: `tg`/curl auth was silently failing (`403`/`401`) because `.env.local`'s `TOGETHER_API_KEY` is double-quoted (`TOGETHER_API_KEY="tgp_v1_..."`) and naive `cut`/`xargs` parsing leaves the quotes in the header value. Strip with `tr -d '"'` (or load via the app's own dotenv, which already handles this correctly) before any raw curl/`tg --api-key` use outside the Next.js runtime.

NEXT: UPDATE (2026-08-09, later same day) - Halim reviewed this finding and explicitly signed off on option (a): a full (non-LoRA) fine-tune of `meta-llama` / Llama-3.1-class open weights on Together, served on a v2 dedicated endpoint at $5.49/GPU-hour with a cost guard (deploy -> prove -> generate 43 -> teardown inside one try/finally, plus an independent shell watchdog). That build is owned by a separate agent, `llama-fulltune`, running `scripts/together-full-sft-run.ts` - NOT this session. Two things already learned there worth carrying forward: `meta-llama/Meta-Llama-3.1-8B-Instruct-Reference` trains (verified, $4.00, job `ft-177f8a45-1335`) but Together then refuses to deploy it - the fine-tune-only object owns no certified deployment config, which is exactly the "trains but cannot be served" trap this verification predicted, just one level deeper (per-base rather than per-product). The script's default base moved to a dynamically-verified one (`assertServableAfterTraining` gates on Together's live `/fine-tunes/models/limits` + v2 registry `PRODUCT_FINE_TUNING` & `PRODUCT_DEDICATED` + certified config, rather than a hardcoded id) after that lesson. Full details and running status live with `llama-fulltune` / `tasks/open-model-options.md`, not here - this entry stays as the closed record of WHY dedicated-endpoint was the only path left.

## Rung C LANDED: OpenAI-tuned Igala model is live in the arena (2026-08-08)

THE UNBLOCK: the Together LoRA (Qwen3-14B, ft-e63efcfc-8670, $4.00) trained but could never be SERVED - tuned Qwen needs a dedicated GPU endpoint and this Together account cannot create one. Retrained the SAME dataset on OpenAI instead, where a tuned model is servable the instant the job finishes. Tuned model now answers all 43 frozen prompts and is in the blind queue.

RUN: DB job `cmsjnjcpn0001jp5nc6s0n9iu` -> OpenAI `ftjob-8KXeP47TjJTjvHUGDqeU2ThV`, base `gpt-4.1-mini-2025-04-14` (snapshot verified against /v1/models), SFT, 573 rows, 3 epochs, 1719 steps, 370,443 trained tokens, **$1.85** billed (priced from reported tokens at $5/1M). Output model `ft:gpt-4.1-mini-2025-04-14:personal:igala-sft:EAQGfuUV`. Auto-registered candidate `cmsjptop00001jpzubdwvttxk` "GPT-4.1 mini SFT (Igala cold-gold)", kind=sft, provider=openai, apiEndpoint=null, archived=false, decoding temp 0. EvalRun `cmsjptp8e0003jpzugxwedhfm` awaiting_human, 43 outputs, $0.02 inference. Dataset grew 318 -> 573 rows as annotators worked; 234 held-out source rows dropped; frozen leakage verified 0 by prompt id AND by prompt text.

PURITY (frozen 43): tuned 0.023 English-share / 0.791 tone-share / 44 chars; gpt-4.1 0.000 / 1.000 / 38; gpt-4.1-mini 0.025 / 1.000 / 44; gpt-4o 0.333 / 0.667 / 444. **Community gold on the same 43: 0.004 / 0.770 / 21.** The tuned model's tone-share collapsed from 1.000 to 0.791 - which is the gold's own 0.770, not a regression: the frontier baselines over-tone-mark relative to how Igala speakers actually write, and SFT moved the model onto the community's orthographic practice. This is exactly the Rung A caveat made visible, and it kills toneShare as a quality signal for tuned models - only blind native judgment settles it now.

KNOWN WEAKNESS: the one English answer out of 43 is the LONG-FORM cultural prompt (ig_cult_001) - the tuned model wrote a full English essay. Training gold is overwhelmingly single words/short sentences (p50 target = 22 chars), so long-form prompts fall back to base behaviour. That is the direct empirical case for Corpus Strategy v2's long-form pivot. ALSO: 56/573 (9.8%) of gold targets contain English inside the first 8 words, because prompts like "give the word for X and for Y" invite bilingual answers ("Egg is ẹ́gẹ and oil is ekpo"). Not a pipeline leak (glosses/rationales are still never read), but it teaches some English framing and should be addressed at PROMPT design, not by filtering community gold.

CODE: new `src/lib/arena/openai-finetune.ts` (real adapter: files -> fine_tuning/jobs -> poll; `buildOpenAiChatRow` is now the single JSONL row builder for BOTH providers, always emits the system turn, assistant turn = Igala gold only). `register-fine-tune.ts` extracted from the poll route and shared with the runner; it fixes a real routing bug - auto-registration used to default provider to "openai-compatible", which for an OpenAI-tuned model would have sent generation to the wrong host with OPENAI_COMPATIBLE_API_KEY (a Together key). `resolveServingRoute` now maps training provider -> serving provider (+ Together's endpoint, closing experiment-1-report blocker 2). `purity.ts` freezes the Rung A metric definitions. Runner: `scripts/openai-sft-run.ts <create|preview|launch|poll|generate|metrics|arena>`, resumable, calls the same libs as the API routes.

ARENA: frozen prompts went 2-3 -> 3-4 outputs (41 at 3, 2 at 4); 43/43 carry a tuned output; 176/258 (68%) of annotator x prompt assignments are now tuned-vs-baseline. Nothing else to do - annotators just work their normal queue.

## Annotation-quality pass, Lydia's review (2026-08-07)

Three features shipped against the "we know models fail, not HOW" problem (690/694 comparisons = both inadequate, so the winner-only rubric almost never fires: 9 rubric rows total). Lydia asked for the full 8-axis rubric on EVERY output; that ~3x's episode time against a 105h annotator budget, so we captured failure diagnostics cheaply instead.

ONE migration, `prisma/migrations/20260807120000_failure_tags_and_dialect`, applied to prod Supabase (`smytgqkgomsfyurskpcl`, schema `wikitongues`) via the management API `apply_migration` (name `failure_tags_and_dialect`), columns verified in information_schema, ledger baselined with `prisma migrate resolve --applied` (6 migrations, "Database schema is up to date"). Additive only: `PairwiseComparison.failureTagsA/failureTagsB TEXT[] NOT NULL DEFAULT '{}'`, `ColdAuthorAnswer.dialect TEXT NULL`.

1. FAILURE TAGS (`src/lib/failure-tags.ts`, CONFIG not schema - retag without a migration). 8 plain-English tags: not_igala, wrong_language, wrong_word, tone_marks, invented, grammar, cultural, english_mixed. Chips render UNDER each output card on the pairwise step: both sides on "both inadequate", the LOSING side only when a winner is picked (`failureTagSides`), never on a tie. Optional, never gates Continue/Submit. ADDITIVE to the still-required English explanation on both-inadequate - the UI says so explicitly. Server drops unknown keys (never 400/500) and re-filters by side, so stale chips can't tag a winner as broken. Pairwise create now uses `select:{id:true}` (RETURNING-all-scalars 500 hazard when prod lags the client).
2. REQUIRED ENGLISH GLOSS. Was optional at 43% adoption (287/666). Now min 3 chars to LOCK a gold answer; both the Lock AND Skip buttons disable while an Igala answer sits unglossed (skip was a side door). Salvage path has its own `salvageGloss` (required only when the rewrite would actually be saved, i.e. not a duplicate of the locked cold answer). `instructionIg` stays optional.
3. DIALECT (`src/lib/dialects.ts`). Select on the gold-answer step, persisted to localStorage `wt-annotator-dialect` so it is a one-time pick. Options PROVISIONAL and commented as such - awaiting Lydia + Agnes before anyone treats it as taxonomy.

RESEARCHER BROWSER: pairwise detail returns per-output `failureTags[{key,label}]` (red chips under each output card, "Marked wrong for"); cold detail now returns `englishGloss` (the gap the earlier reviewer flagged) + `dialect`/`dialectLabel`. Response shape additive; CSV export column list untouched.

GATES: typecheck 0, lint 0, test 173/173 (17 files; new `failure-tags.test.ts` 19 + `dialects.test.ts` 11), `next build` OK. NOTE: `npm run lint` was ALREADY failing on main - `prisma/seed-prompt-longform.ts` has a plain helper `useWord()` that react-hooks/rules-of-hooks reads as a hook. Fixed by scoping that rule off `prisma/**/*.ts` in eslint.config.mjs (those are Node seed scripts, zero React).

## Corpus Strategy v2 + open questions resolved (2026-07-31)

DOMAIN WAR ENDED: marketing Vercel project renamed wikitongues-web-ai (via API PATCH; CLI rename no-ops), owns wikitongues-web-ai.vercel.app as project domain (auto-updates on its deploys); wikitongues-ai-web.vercel.app detached from it and re-aliased to the app - annotators' URL is permanently safe. Marketing /research live pending Halim disabling Vercel Deployment Protection (SSO) in dashboard. Research tab: full story + 3 SVG diagrams + stats grid auto-fed by GET /api/public/stats (aggregate-only, no PII, 300s cache).

FINE-TUNE vs ALTERNATIVES (Halim's singulars question, answered): prompting is maxed and failed on content (99% inadequate WITH the forcing prompt) - missing knowledge enters via weights (SFT) or context (RAG), DPO is the finisher not the teacher. Resolution is empirical: arena carries all strategies as candidates - (1) LoRA'd open models (Qwen3-14B training now: only open weights are tunable; strongest multilingual open model steerable by ~318 rows; Kimi K2 too big to tune; Opus/GPT/Gemini closed), (2) frontier + system prompt, (3) frontier + RAG over gold (the singulars-style bet, buildable now), (4) OpenAI-API-tuned gpt-4.1-mini (same JSONL, feasible). Blind native judgment referees.

AUTORATER (answered): buildable today as triage, never as replacement - (1) contamination/language-ID gate validated against 420+ human labels, (2) gold-anchored diacritic-aware similarity for the 108 gold-covered prompts (coverage grows with corpus), (3) LLM-judge format triage (arena judge scaffold exists). Ceiling: no model can judge Igala quality it cannot speak - every autorater version must ship with a human-agreement score.

CORPUS STRATEGY v2 (the plan): answered gold skews to single words/sentences (orth 139 / gram 87 / lex 28) - spelling anchors, not language. Pivot to: (1) LONG-FORM generative prompts (folktales, procedures, descriptions, advice; 3-10 sentence answers; steering metric = Igala tokens per annotator-hour, not episodes); (2) COMMUNITY-AUTHORED prompts IN Igala via Agnes's team (kills English-instruction monoculture per EACL findings, produces the community-authored held-out benchmark the methodology requires); (3) FLOATING-MOTIF set, 30-50 prompts from Berezkin's Africa-attested motifs (Andy's thread; feeds paper E3 + Emily's 3 benchmark questions); (4) COVERAGE MATRIX (9 domains x length x register) audited monthly against gold via the searchable annotations browser; (5) enrichment follow-ups ("use it in two sentences") on verified single-word gold; (6) dialogue/multi-turn phase after long-form lands. SEQUENCING: Lydia orthography-convention session FIRST (spelling divergence compounds in long text - the calibration finding), then long-form + motif prompts, then community prompt drive, then retrain (run 2) = the October demo model. Discipline unchanged: cold-first, Igala-only completions, English in metadata, contamination+diacritic gates, freeze-before-train.

IN FLIGHT: Together LoRA ft-e63efcfc-8670 (Qwen3-14B, 318 rows) training; runner auto-resumes for generation + purity + blind arena. Gmail drafts (Google group / Nicolai / Darren) awaiting Halim review. NotebookLM "Igala - Wikitongues Research" holds 32 sources incl. full context.
